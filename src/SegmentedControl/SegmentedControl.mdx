import { Canvas, Controls, Primary, Meta, Subtitle, Title } from '@storybook/blocks';
import * as SegmentedControl from './SegmentedControl.stories';

<Meta of={SegmentedControl} />
<Title>Components/SegmentedControl</Title>
<Subtitle>Basic Segmented Buttons Control</Subtitle>

<Primary />
<Controls />

### No Label

<Canvas of={SegmentedControl.NoLabel} />

### With Description

<Canvas of={SegmentedControl.Description} />

### Disabled

## One Disabled

Segmented buttons supports disabling individual options

<Canvas of={SegmentedControl.OneDisabled} />

## All Disabled

Segmented buttons supports disabling all options

<Canvas of={SegmentedControl.AllDisabled} />

## Sizes

Segmented buttons are available in small & medium (default)

### Small

<Canvas of={SegmentedControl.Small} />

### Medium

<Canvas of={SegmentedControl.Medium} />
